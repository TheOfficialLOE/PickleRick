
export default ({ className }: { className: string }) => {
    return (
        <img
            src="pickle-rick.gif"
            alt="Pickle Rick"
            className={className}
        />
    );
};
